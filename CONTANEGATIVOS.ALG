algoritmo "CONTANEGATIVOS"
var
   N, c, totN: INTEIRO
inicio
      totN <- 0
      c <- 1
   Repita
      Escreva ("Digite um numero: ")
      Leia (n)
           Se (N < 0) entao
              TotN <- Totn + 1
           FimSe

      C <- c + 1
   Ate (C > 5)

   EscrevaL ("Foram digitados ", TotN, " valores negativos")

fimalgoritmo